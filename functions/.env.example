# Firebase Functions 환경 변수 템플릿
# 이 파일을 복사해서 .env.local (로컬용) 과 .env.prod (운영용) 를 만드세요.
# .env.local / .env.prod 는 gitignore 되어 있으므로 외부에 노출되지 않습니다.

# ── 카카오 공유 SDK (선택) ─────────────────────────────────────
# 카카오 개발자 콘솔 > 내 애플리케이션 > 앱 키 > JavaScript 키
# KAKAO_JS_KEY=

# ── 기타 외부 API ─────────────────────────────────────────────
# 필요한 키를 여기에 추가하세요.
